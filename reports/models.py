# This app is primarily read-only / aggregation views over other apps'
# models (GeneratedPaper, PaperSubmission, Payment). No new tables needed;
# kept as a package so report-specific models (e.g. saved filters) can be
# added later without restructuring the project.
